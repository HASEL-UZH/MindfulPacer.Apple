disabled_rules:
    - line_length
    - function_parameter_count
    - trailing_whitespace

type_body_length:
    warning: 200
    error: 300

type_name:
    min_length: 3
    max_length: 50

type_body_length:
    max_length: 300
